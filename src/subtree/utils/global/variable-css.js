/**
 * @author 🌈先知云 <公众号：先知云，微信：zhl632231327>
 * @date ⌚2020/12/16
 * @description 📝 全局变量-css变量
 */

export default {
    // TODO 先知云(暂停使用，需要优化)  @scope 弹窗  宽度 (暂停使用，需要优化)
    dw_col: '430px', // 弹窗的宽度，一列input输入框
    dw: '800px', // 弹窗的宽度，仅仅是在两列input输入框
    dw2: '850px', // 弹窗中嵌套table中，table中的显示2行列
    dw3: '900px', // 弹窗中嵌套table中，table中的显示2行列
    dw4: '950px', // 弹窗中嵌套table中，table中的显示2行列
    dw5: '1000px', // 弹窗中嵌套table中，table中的显示2行列

    // TODO 先知云(暂停使用，需要优化)  @scope 列表  树形结构和表格的宽度
    fixed_l: 200, // 左侧列固定宽度
    fixed_r_p1: 100, // 右侧列固定宽度
    fixed_r_p1_2: 120,
    fixed_r_p2: 200,
    fixed_r_p3: 300,
    fixed_r_p4: 500,

    // TODO 先知云(暂停使用，需要优化)  @scope 操作列  树形结构和表格的宽度
    op_1: 80,
    op_2: 100,
    op_3: 140,
    op_4: 200
}
